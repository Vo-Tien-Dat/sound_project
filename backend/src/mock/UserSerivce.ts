import { User } from 'src/models/interface/user.interface';

export class UserService {
    create(u) {
        return {
            userId: '1',
            userName: 'hello world',
        };
    }
}
